import React,{useState} from 'react';

function HShow() {
  // eslint-disable-next-line
  const[isShow, setShow] = useState(false);
  
  /* if (isShow){
    console.log(e);
    e.target.style.display = 'none';
    document.getElementById('eye').display = 'block';
    document.getElementById('inpSenha').setAttribute('type', 'password');
    setShow(false);
   }else{
    e.target.style.display = 'none';
    document.getElementById('eyeUnview').removeAttribute('style');
    document.getElementById('inpSenha').setAttribute('type', 'text');
    setShow(true) 
  }
  alert(isShow);
 */
  return(<div></div>)
} 

export default HShow;